# XomChieu v25

Bản đầy đủ được tạo từ Git commit `855211c`.

Thay đổi:

- Sửa lỗi TypeScript ở ảnh chi tiết thành tích.
- Cho phép bấm ảnh trong chi tiết thành tích để phóng to.
- Xóa file component bị trùng tên sai `src/components/DetIailHeroImage.tsx`.
- Sửa số lượt xem bài viết bị cộng dư 1 trên cửa sổ chi tiết.

Đã kiểm tra:

- `npm run lint`
- `npm test` — 6/6 đạt
- `npm run build`

Xem báo cáo đầy đủ ở file `AUDIT-REPORT-v25.md` được cung cấp kèm gói.
